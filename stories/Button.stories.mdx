import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Button, Props } from '../src/Button';

<Meta title="MDX/Button" component={Button} />

export const Template = (args) => <Button {...args} />;

# Button

Let's define a story for our `Button` component:

<Story name="My Button" args={{ children: 'I am in MDX', variant: 'primary' }}>
  {Template.bind({})}
</Story>

<ArgsTable story="My Button" />

### `canvas for secondary Button`

<Canvas>
  <Story
    name="Secondary"
    args={{ children: 'Secondary MDX', variant: 'secondary' }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
